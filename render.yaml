services:
  - type: web
    name: aviator-predictor-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class eventlet -w 1 live_predictor_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
